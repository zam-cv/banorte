class Config {
  // URL base del backend
  static const String baseUrl =
      'https://9c2d-201-98-213-10.ngrok-free.app'; // Cambia la URL si es necesario

  // Token de autenticación (para almacenar el token una vez que el usuario inicie sesión)
  static String? token;
}
